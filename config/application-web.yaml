server:
  port: 8089
  mode: release
  timeout: 30s
  password: admin  # 临时保留，后续移除

log:
  path: logs/app
  level: INFO

database:
  type: sqlite  # mysql, postgres, sqlite
  host: localhost
  port: 3306
  username: root
  password: ""
  database: go_web_admin
  ssl_mode: disable
  # SQLite配置（当type为sqlite时使用）
  path: data/app.db

redis:
  host: localhost
  port: 6379
  password: ""
  database: 0

jwt:
  secret: "your-secret-key-change-in-production"
  expire: 24h
  refresh_ttl: 168h  # 7天
